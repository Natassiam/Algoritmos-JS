/* let nome = 'Anna'; */
let acumulador = 0;
for(let iterador = 0; iterador <= 10; iterador+=1){
  
  acumulador = acumulador + iterador + 10;
/*   acumulador += iterador */
console.log(acumulador);
}