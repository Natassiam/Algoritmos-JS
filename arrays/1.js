const array = ['manga', 'melancia', 'pitomba', 'bacuri', 'jambo'];
/* console.table(array) */
/* console.log(array.length); */
for(let index = 0; index <= array.length; index+=1){
  if(array[index] === 'melancia') {
    console.log(`Não gosto de ${array[index]}`);
  }
}
let soma = 0;
const numerosMarotos = [1, 4, 6.7, 8, 9];
for (let index = 0; index < numerosMarotos.length; index++) {
  soma = soma + numerosMarotos[index];
console.log(soma);

}

let nome = 'Natassia';
console.log(nome.length );


